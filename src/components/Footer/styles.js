import { makeStyles } from "@material-ui/core";

export default makeStyles( () => ({
    footer: {
        paddingTop: '60px',
        paddingBottom: '20px',
        display: 'flex',
        justifyContent: 'center'
    }
}))